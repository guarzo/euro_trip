_site/
site/_site/
.sass-cache/
.jekyll-cache/
site/.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
# Gemfile.lock IS committed: CI uses ruby/setup-ruby with bundler-cache,
# which needs it, and a deployed site wants reproducible gem versions.
*.swp
*.swo
.DS_Store
